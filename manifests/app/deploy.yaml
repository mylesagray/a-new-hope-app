apiVersion: v1
kind: Namespace
metadata:
  name: flower-market
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: a-new-hope-dashboard
  labels:
    app: flower-counter
  namespace: flower-market
spec:
  replicas: 1
  selector:
    matchLabels:
      app: a-new-hope-dashboard
  template:
    metadata:
      labels:
        app: a-new-hope-dashboard
    spec:
      containers:
      - name: dashboard
        image: vhojan/a-new-hope-dashboard:latest
        ports:
        - name: http
          containerPort: 80
        resources:
          requests:
            ephemeral-storage: 10M
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: a-new-hope-worker
  labels:
    app: flower-counter-worker
  namespace: flower-market
spec:
  selector:
    matchLabels:
      app: a-new-hope-worker
  template:
    metadata:
      labels:
        app: a-new-hope-worker
    spec:
      containers:
      - name: worker
        image: vhojan/a-new-hope-worker:latest
        ports:
        - name: metrics
          containerPort: 8080
        env:
          - name: DASHBOARD_FQDN
            value: "flower-dashboard.flower-market"
          - name: BATCHES
            value: "2000"
          - name: PARTIAL_GPU
            value: "0.5"
          - name: NUM_GPUS
            value: "1"
        resources:
          requests:
            ephemeral-storage: 10M
            memory: "16Gi"
          limits:
            memory: "16Gi"            
---
apiVersion: v1
kind: Service
metadata:
  name: flower-dashboard
  labels:
    app: flower-counter
  namespace: flower-market
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: a-new-hope-dashboard
---
apiVersion: v1
kind: Service
metadata:
  name: flower-metrics
  labels:
    app: flower-counter-worker
  namespace: flower-market
spec:
  type: LoadBalancer
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
  selector:
    app: a-new-hope-worker
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: flower-dashboard-fps
  labels:
    app: flower-counter-monitor
  namespace: monitoring
spec:
  endpoints:
    - targetPort: 80
      interval: 5s
  namespaceSelector:
    matchNames:
    - flower-market
  selector:
    matchLabels:
      app: flower-counter
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: flower-worker-fps
  labels:
    app: flower-counter-monitor
  namespace: monitoring
spec:
  endpoints:
    - targetPort: 8080
      interval: 5s
  namespaceSelector:
    matchNames:
    - flower-market
  selector:
    matchLabels:
      app: flower-counter-worker
---
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: flower-autoscaler
  labels:
    app: flower-counter
  namespace: flower-market
spec:
  maxReplicas: 6
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: a-new-hope-worker
  metrics:
  - type: External
    external:
      metric:
        name: aggregate
      target: 
        type: Value
        value: '10000'